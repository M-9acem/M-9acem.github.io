import React from 'react'; import Reveal from './Reveal'; import SectionHeader from './SectionHeader'; import { publications, researchProjects } from '../data/siteData';
export default function Research(){return <section id="research"><div className="container"><SectionHeader kicker="Research" title="Papers, findings and open questions.">Published work and ongoing studies at the intersection of decentralized learning and LLM safety.</SectionHeader><div className="publications">{publications.map(p=><Reveal className="publication" key={p.title}><div className="pub-venue">{p.venue}</div><h3>{p.title}</h3><p>{p.authors} — {p.text}</p></Reveal>)}</div><div className="research-projects">{researchProjects.map(r=><Reveal className="research-card" key={r.name}><div className="meta">{r.date} · Research project</div><h3>{r.name}</h3><p className="tagline">{r.tagline}</p><ul>{r.points.map((pt,i)=><li key={i}>{pt}</li>)}</ul><div className="tag-row">{r.tech.split(', ').map(t=><span className="tag" key={t}>{t}</span>)}</div></Reveal>)}</div></div></section>}
