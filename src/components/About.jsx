import React from 'react'; import Reveal from './Reveal'; import SectionHeader from './SectionHeader'; import { stats } from '../data/siteData';
export default function About(){return <section id="about"><div className="container"><SectionHeader kicker="About" title="Research mindset. Engineering execution.">I like problems where machine learning meets systems and safety: decentralized learning, LLM alignment, communication constraints and research that has to hold up outside a notebook.</SectionHeader><div className="stats">{stats.map(([value,label])=><Reveal className="stat" key={value}><strong>{value}</strong><span>{label}</span></Reveal>)}</div></div></section>}
