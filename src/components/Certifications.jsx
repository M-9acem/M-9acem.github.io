import React from 'react'; import Reveal from './Reveal'; import SectionHeader from './SectionHeader'; import { certifications } from '../data/siteData';
export default function Certifications(){return <section id="certifications"><div className="container"><SectionHeader kicker="Certifications" title="Always learning.">Selected industry credentials across data engineering, cloud and AI.</SectionHeader><div className="credential-grid">{certifications.map(c=><Reveal className="credential" key={c.title}><div className="credential-head"><div className="credential-logo"><img src={c.logo} alt={`${c.issuer} logo`}/></div><div><div className="issuer">{c.issuer}</div>{c.status&&<span className="status">{c.status}</span>}</div></div><h3>{c.title}</h3><p>{c.text}</p></Reveal>)}</div></div></section>}
